# Copilot Instructions for file-modifier

## Repository Overview

`file-modifier` is a minimal repository currently in its initial state. It contains only a `README.md` at the root. There is no application code, build system, test framework, or CI pipeline configured yet.

## Repository Layout

```
/
├── README.md          # Project title only ("# file-modifier")
└── .github/
    └── copilot-instructions.md  # This file
```

## Build, Test & Lint

There are currently **no** build, test, lint, or CI scripts configured in this repository. When adding them, document the commands here so future agents can use them without searching.

## Guidelines for Copilot Coding Agent

- This repository is a blank slate. When adding features, create appropriate source files, tests, and configuration as needed.
- Always check the `README.md` for any updated project description before starting work.
- When a build system, test framework, or linter is introduced, update this file with the exact commands to run them.
- Prefer small, incremental changes and validate each step before proceeding.
- Trust the instructions in this file; only search the codebase if information here is incomplete or incorrect.
