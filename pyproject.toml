[tool.ruff]
target-version = "py39"
line-length = 120
exclude = [
    "vendor-source/",
    "rendered/",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
